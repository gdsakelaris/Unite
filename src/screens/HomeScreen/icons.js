//icons for HomeScreen
import { fontScale, colors } from '../../base';
import renderExpoIcon from '../../utils/renderExpoIcon';
export const locationIcon = renderExpoIcon('Ionicons', 'ios-location-sharp', {size: 24 / fontScale, color: colors.lightorange})
