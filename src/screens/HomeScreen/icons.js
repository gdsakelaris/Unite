import { Ionicons } from '@expo/vector-icons';
import { fontScale, colors } from '../../base';
export const locationIcon = <Ionicons name="ios-location-sharp" size={ 24 / fontScale} color={colors.lightorange}/>