import renderExpoIcon from "../../utils/renderExpoIcon";
import { fontScale, colors } from "../../base";
import { inputBox as styles } from "./css";
export const eye = renderExpoIcon('Ionicons', 'md-eye', {size: 20/ fontScale, color: colors.lightgray})
export const eye_off = renderExpoIcon('Ionicons','md-eye-off',{size: 20/ fontScale, color: colors.lightgray})
